<?xml version="1.0" encoding="utf-8"?>
<!--
    Creative Space legacy backup rules (API < 31).
    Preserves user creative work across device migrations.
    Only included paths are backed up; cache is excluded by omission.
-->
<full-backup-content>
    <include domain="file" path="collections/" />
    <include domain="file" path="embers/" />
</full-backup-content>